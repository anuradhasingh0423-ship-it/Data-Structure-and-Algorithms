# Example: Built-in sorting functions like Python's .sort() (which uses Timsort), Merge Sort, or Quick Sort.

# Slightly worse than linear, but still highly efficient.
#  This is the optimal time complexity for general-purpose sorting algorithms.